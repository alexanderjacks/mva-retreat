import styled from 'styled-components';

const H2 = styled.h2`
  color: #3e465b;
  font-size: calc(1.15vw + 1.15vh + 1.15vmin);
  font-weight: 700;
  line-height: 1.5;
  margin: 25px 50px 15px;
`;

export default H2;