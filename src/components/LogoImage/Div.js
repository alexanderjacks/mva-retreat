import styled from 'styled-components';

const Div = styled.div`
  height: auto;
  margin: 0px auto 0px auto;
  overflow: hidden;
  width: 100vw;

  img {
    padding: 0px;
    border-radius: 0%;
  }
`;

export default Div;